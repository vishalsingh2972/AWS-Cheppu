# Root .env is used by docker-compose.yml (env_file: ./.env)
# For local Python runs, prefer backend/.env (used by start.ps1 / start.sh)
#
# Copy backend/.env.example → backend/.env for local testing
# OR copy this file to .env for Docker:
#   cp backend/.env.example .env
